### get_response_image(pil_img, format)

Convert PIL Image to base64 encoded string in the specified format.

**Parameters:**
- pil_img (PIL.Image): The image to convert.
- format (str): The desired image format (e.g., 'PNG', 'JPEG').
